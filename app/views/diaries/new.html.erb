<div class="form-container">
  <div class="form-new-diary">
    <%= simple_form_for @diary, html: { autocomplete: "off" } do |f| %>
      <%= f.input :title, label: "Title" %>
      <%= f.input :private %>
      <%= f.button :submit %>
    <% end %>
  </div>
</div>
